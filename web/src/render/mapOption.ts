// Re-export do módulo compartilhado — a implementação única vive em shared/mapOption.js
// (usada também pelos apps publicados via bundle StudioRuntime).
export { buildMapOption } from '../../../shared/mapOption.js';
